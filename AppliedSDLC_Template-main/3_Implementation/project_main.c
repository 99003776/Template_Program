#include "AREA.h"


int main()
{
    double radius;
    long result;
    int num1,num2;
   
    area_circum(radius);
    rect_area(num1,num2);
    square_area(num1);
    triangle_area(num1,num2);
    return 0;
}